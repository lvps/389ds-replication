---
server_uri: "ldap://localhost"
rootdn: "cn=Directory Manager"
#rootdn_password: secret
use_starttls: true
tls_certificate_public_trusted: true

serverid: default
suffix: dc=example,dc=com

# Example used throughout section 15 of the Administration Guide
replication_user: Replication Manager
#replication_user_password: even-more-secret
begin_replication_immediately: true

#####################
# Supplier settings #
#####################

supplier_replica_id: 1 # For suppliers: 1 to 65534
#consumer_uri: "ldap://consumer.example.com:389/"
#replication_user_remote: Replication Manager
#replication_user_password_remote: even-more-secret

#####################
# Consumer settings #
#####################

#consumer_referral_to_supplier: "ldap://supplier.example.com:389/"
